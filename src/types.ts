export type TextDesign = {
  font: string;
  color: string;
  text: string;
  colorPickerOpen: boolean;
  fontSize: number;
};

export type ColorPickerDesign = {
  color: string;
  colorPickerOpen: boolean;
};
